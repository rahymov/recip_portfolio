<div class="row">
  <div class="col-md-6">
    <% @recipes.each do |recipe| %>
      <h2><%= link_to recipe.name, recipe_path(recipe) %></h2>
      <p><%= recipe.description %></p>
    <% end %>
  </div>
</div>
